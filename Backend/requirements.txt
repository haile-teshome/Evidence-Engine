# --- HTTP API layer (new) ---
fastapi==0.115.0
uvicorn[standard]==0.31.0
python-dotenv==1.0.1
python-multipart==0.0.9
# --- HTML/XML parsing ---
# (Streamlit + graphviz removed: the API uses a headless fake-streamlit shim,
#  so the heavy/fragile streamlit package is no longer required.)
beautifulsoup4==4.12.3
lxml==5.3.0
# --- AI & LLM (Conflict-Matched Versions) ---
langchain==0.3.0
langchain-core==0.3.0
langchain-openai==0.2.0
langchain-anthropic==0.2.0
langchain-ollama==0.2.0
langchain-google-genai
openai==1.52.2
anthropic==0.34.1
ollama==0.3.3
google-generativeai
# --- Data & Document Processing ---
pandas==2.2.2
biopython>=1.85
pypdf==4.3.1
pdfplumber==0.11.4
python-docx==1.1.2
numpy==1.26.4
# --- Screening prioritisation (active-learning mode) ---
# These are what make the shipped ranker the same ranker the benchmark measured
# (WSS@95 0.679 over the 26 SYNERGY reviews; see Backend/ranking.py). They are
# heavy: torch plus the BGE-large weights come to roughly 1.6 GB, and the
# weights are fetched separately and explicitly via POST /api/rank/warm rather
# than at install time.
#
# Without them the app still ranks, but through a weaker model that carries no
# published figure. It reports itself as the "tfidf" or "nb" tier and the UI
# says so, which is the point: the benchmarked and shipped components came apart
# once already because a downgrade was silent.
scikit-learn>=1.4
torch>=2.2
transformers>=4.40
huggingface_hub>=0.23

# --- Critical Dependency Fixes ---
packaging==24.1
requests==2.32.3
urllib3==1.26.19
certifi==2024.8.30
pydantic==2.9.2
keyring>=24

# --- Optional: UniTable image-based table recognition (enable with EE_USE_UNITABLE=1) ---
# torch
# torchvision
# tokenizers
# einops
# beautifulsoup4
