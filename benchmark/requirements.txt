# Reuse the backend's LLM clients (langchain etc.). Add a few benchmark-specific deps.
-r ../Backend/requirements.txt

# Tabular metrics + reporting
scikit-learn>=1.5
tabulate>=0.9
pyyaml>=6.0
tqdm>=4.66

# Optional: Modal for GPU-accelerated inference (set MODAL_TOKEN_ID + MODAL_TOKEN_SECRET)
modal>=0.66
